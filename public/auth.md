# Authentication

This is a public portfolio site and does not currently offer protected APIs or require agent authentication. However, if protected resources become available, agents can register following the OAuth 2.0 standards at our endpoints.

## Agent Registration
Agent registration would be handled via the standard `/oauth/register` endpoint specified in our `/.well-known/oauth-authorization-server` metadata.
